{
  "version": 2,
  "builds": [
    { "src": "server.cjs", "use": "@now/node" }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "frontend/static/$1" },
    { "src": "/(.*)", "dest": "server.cjs" }
  ]
}
